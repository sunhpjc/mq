<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunhp.rocketmq.dao.SmsDao">

    <resultMap type="com.sunhp.rocketmq.entity.Sms" id="SmsMap">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="TINYINT"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" parameterType="java.lang.Long" resultMap="SmsMap">
        select
          id, phone, content, status
        from springboot.sms
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SmsMap">
        select
          id, phone, content, status
        from springboot.sms
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SmsMap">
        select
          id, phone, content, status
        from springboot.sms
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into springboot.sms(phone, content, status)
        values (#{phone}, #{content}, #{status})
    </insert>
    <insert id="insertBatch" parameterType="java.util.ArrayList">
        insert into sms(phone, content, status)
        values 
        <foreach collection="list" index="index" item="item" separator=",">
            (#{item.phone},#{item.content},0)
        </foreach>
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update springboot.sms
        <set>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from springboot.sms where id = #{id}
    </delete>

</mapper>